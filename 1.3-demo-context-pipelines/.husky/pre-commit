#!/bin/sh

# Video 1.3: Automated Context Pipelines
# This pre-commit hook regenerates the project index before each commit,
# ensuring AI tools always have up-to-date context.

echo "Regenerating project index..."
node scripts/generate-index.mjs
git add PROJECT_INDEX.md
