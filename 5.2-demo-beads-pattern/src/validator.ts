/**
 * Bead 2: User Validator
 * STUB — implement during the Beads Pattern demo (after Bead 1 passes).
 */
import { ValidationResult } from './types';

export function validateUsers(_users: Array<Record<string, string>>): ValidationResult {
  throw new Error('Not implemented — Bead 2 task');
}
