/**
 * Bead 3: Deduplicator
 * STUB — implement during the Beads Pattern demo (after Bead 2 passes).
 */
import { User, DeduplicationResult } from './types';

export function deduplicateUsers(_users: User[]): DeduplicationResult {
  throw new Error('Not implemented — Bead 3 task');
}
